# ICSDC Frontend

This project is now a Node.js application that serves the static frontend files in this repository.

## Requirements

- Node.js 18+

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:3000` in your browser.

You can set a custom port with:

```bash
PORT=8080 npm start
```
